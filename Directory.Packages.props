<Project>

  <PropertyGroup>
    <!-- 已关闭中心化包管理（CPM）。各项目在自己的 .csproj 中通过 Version 属性独立管理包版本，
         GUI 升级包时只影响当前项目，不会联动其他项目。 -->
    <ManagePackageVersionsCentrally>false</ManagePackageVersionsCentrally>
  </PropertyGroup>

</Project>